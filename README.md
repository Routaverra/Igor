# igor

A constraint programming DSL for Clojure, backed by MiniZinc.

## Roadmap

- [ ] `minimize` — explicit minimization API (currently requires negated `maximize`)
- [ ] Solver time limits
- [ ] Search annotations (variable/value ordering hints)
- [ ] Solution iteration (find N diverse solutions)
